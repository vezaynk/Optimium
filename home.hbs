{{! Import defaut template and home header}}
{{!< default}}{{> header}}


<a name="start"></a>
<div id="content_column_Optimium" class="home-listing boxed-cargo">
<center>
				
	{{! Featured posts come first }}
	{{! Cycle through and list featured posts }}
	{{! Only display if not blocked by exclusion tags }}
	{{#foreach posts}}{{#if featured}}{{^has tag="hidden, no-home, tag-only, author-only, no-index, private"}}
				
		
		{{>listing}}
			
			{{/has}}{{/if}}{{/foreach}} {{! For validation}}
			
			
			{{! ============================================================================================ }}
			{{! ============================================================================================ }}
			{{! ============================================================================================ }}
			{{! ============================================================================================ }}
			{{! ============================================================================================ }}
			{{! ============================================================================================ }}
			
	{{! Cycle through and list non-featured posts }}
	{{! Only display if not blocked by exclusion tags }}
	{{#foreach posts}}{{^if featured}}{{^has tag="hidden, no-home, tag-only, author-only, no-index, private"}}
				
		
		{{>listing}}
			
			{{/has}}{{/if}}{{/foreach}} {{! For validation}}
			
			 </div>
			 {{pagination}}